    1  vcgencmd get_throttled
    2  vcgencmd -n1 get_throttled
    3  watch -n1 vcgencmd  get_throttled
    4  sudo apt update
    5  sudo reboot
    6  ping google.com
    7  ip addr
    8  hostname -I
    9  ip addr
   10  hostname -I
   11  ip route
   12  sudo tcpdump -i et0
   13  sudo apt install tcpdump
   14  sudo tcpdump -i et0
   15  sudo tcpdump -i eth0
   16  ip addr
   17  sudo ip addr add 192.168.50.1 dev eth0
   18  sudo ip link set eth0 up
   19  sudo sysctl -w net.ipv4.ip_forward=1
   20  ip route
   21  sudo iptables -t nat -A POSTROUTING -o wlan0 -j MASQUERADE
   22  sudo iptables -t nat -D POSTROUTING -o wlan0 -j MASQUERADE
   23  sudo ip addr del 192.168.50.1/24 dev eth0
   24  ip addr show eth0
   25  ip addr 
   26  sudo apt install dnsmasq
   27  sudo apt install vim
   28  vim /et/dnsmasq.conf
   29  vim /etc/dnsmasq.conf
   30  sudo vim /etc/dnsmasq.conf
   31  sudo ip addr add 192.168.50.1/24 dev eth0
   32  sudo sysctl -w net.ipv4.ip_forward=1
   33  sudo iptables -t nat -A POSTROUTING -o wlan0 -j MASQUERADE
   34  sudo systemstvl restart dnsmasq
   35  sudo systemctl restart dnsmasq
   36  sudo tcpdump -i eth0
   37  tailscale status
   38  sudo tailscale down
   39  sudo tailscale status
   40  sudo tailscale up --exit-node=100.84.109.9
   41  sudo tailscale status
   42  sudo tailscale down
   43  sudo tailscale up --exit-node=100.84.109.9
   44  iptable
   45  iptables
   46  iptables -h
   47  sudo iptabled -L -n -v
   48  sudo iptables -L -n -v
   49  sudo iptables -t nat -A POSTROUTING -o tailscale0 -j MASQUERADE
   50  sudo iptables -L -n -v
   51  cat /etc/dnsmasq.conf
   52  sudo tailscale down
   53  sudo tailscale up --exit-node=100.84.109.9 --exit-node-allow-lan-access
   54  ip route
   55  tailsacle status
   56  tailscale status
   57  ip route show table 52
   58  tailscale debug prefs
   59  sudo tailscale down
   60  sudo tailscale up --exit-node=100.84.109.9 --exit-node-allow-lan-access --accept-routes
   61  ip route show table 52
   62  tailscale debug prefs
   63  ip route
   64  tailscale debug prefs
   65  ip rule
   66  ip route how table 52
   67  ip route show table 52
   68  touch terminal-history
   69  history > terminal-history 
